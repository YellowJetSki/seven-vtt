# Asset Directories for VTT
# Place PNG images in the appropriate folder based on suffix:
#   _portrait.png → portraits/
#   _bm.png       → tokens/
#   _enc.png      → maps/
#   _item.png     → items/
# These are served at runtime via /images/{category}/{filename}
