{
  "name": "str-vtt-root",
  "private": true,
  "description": "Monorepo root — VTT project lives in /vtt",
  "scripts": {
    "build": "cd vtt && npm run build",
    "dev": "cd vtt && npm run dev",
    "lint": "cd vtt && npm run lint",
    "deploy": "cd vtt && npx vercel --prod"
  },
  "dependencies": {
    "source-map": "^0.7.6"
  }
}
