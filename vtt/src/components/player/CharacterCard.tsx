/* ── CharacterCard (v3 — Re-export) ─────────────────────────────
 * Re-exports the premium card from the /card/ subdirectory
 * to maintain backward compatibility with existing imports.
 * ─────────────────────────────────────────────────────────────── */

export { CharacterCard } from "@/components/player/card/CharacterCard";
